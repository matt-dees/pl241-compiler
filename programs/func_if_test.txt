//Basic if statement test
main
procedure foo( );
var a, b;
{
	let a <- call InputNum();
	let b <- 2;
	if a > b then
		let a <- a + 1
	else
		let b <- b + 1
	fi;
	
	if a < b then
		let a <- a - 1
	fi;
	
	call OutputNum(a);
	call OutputNum(b)
};
{
    call foo()
}.